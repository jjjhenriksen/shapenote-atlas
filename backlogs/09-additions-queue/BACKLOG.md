# P2 — Reconcile 2025 additions with score and review queues

## Goal

`/goal` Make the relationship between the publisher's 2025 additions list, corpus records, exact scores, references, drafts, and review-only items explicit and validator-enforced.

## Current evidence

- `edition-2025-additions.json` lists 113 additions, all mapped to corpus records.
- Exact, reference, and draft presence overlap; the current categories are difficult to interpret without precedence rules.

## Work

- Define whether each record is an addition, shared revision, or current-index record and preserve that distinction.
- Explain every overlap among exact score, alternate reference, OMR draft, image review, and transcription queue.
- Detect additions missing from the queue and non-additions incorrectly treated as new songs.
- Add invariant checks for counts, identity, and status precedence.

## Acceptance

- Every listed addition has one explicit corpus identity and queue disposition.
- Every overlap is either intentional and documented or fixed.
- Counts are reproducible from source files and validators fail on unexplained drift.

## Ownership

Own additions metadata, queue joins, and related validators. Do not alter notation assets or source images.

## Agent-07 audit snapshot (2026-08-30)

The isolated read-only audit is `work/agent-07-additions/agent-07-additions-audit.json`, generated by `scripts/agent_07_additions_audit.py` and covered by `tests/test_agent_07_additions_audit.py`. It does not write `public/` data, notation assets, source images, UI files, or active 115/116 records.

- 590 current SH25 records reconcile to 113 additions and 477 non-additions; all 113 addition identifiers are present and the known superseded `sh2025/264b` is absent. One preserved legacy record remains: `sh2025/414b` / “Farewell Brethren”; current `sh2025/414b` is “Parting Friend”.
- Strict source disposition is 13 exact SH25 source witnesses pending correction, 13 reference-only additions, and 87 source-only/draft additions. No draft is treated as complete or promotion-safe.
- The current corpus exposes 25 exact-score rows, but 12 are source-audit `external-source-blocked` alternate witnesses and therefore cannot be counted as exact SH25 notation. The source-audit catalog also retains a non-current duplicate identity `sh2025/54-as-written`.
- The raw corpus queue is internally consistent (88 addition rows; 25 structured-score exclusions), but strict source/reference semantics reduce legitimate structured-score exclusions to 13 and expose a 12-record queue gap: `sh2025/160t`, `178b`, `27b`, `308`, `438`, `452t`, `467`, `497b`, `50b`, `51`, `515`, and `54`. These must remain reference-only until an edition-matched SH25 source is acquired; no draft is complete.
